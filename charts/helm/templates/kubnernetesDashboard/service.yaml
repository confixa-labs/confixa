{{- if .Values.kubernetesdashboard.enable }}
apiVersion: v1
kind: Service
metadata:
  labels:
    k8s-app: {{ .Values.kubernetesdashboard.name }}
  name: {{ .Values.kubernetesdashboard.name }}
  namespace: {{ .Release.Namespace }}
spec:
  ports:
    - port: {{ .Values.kubernetesdashboard.service.port }}
      targetPort: {{ .Values.kubernetesdashboard.service.targetPort }}
  selector:
    k8s-app: {{ .Values.kubernetesdashboard.name }}
{{- end }}